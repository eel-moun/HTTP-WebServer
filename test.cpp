#include "iostream"
#include <string>
#include <fstream>
#include <exception>
using namespace std;

int main()
{
    std::string ttt = "";

    // std::cout << test.substr(0, 5) << std::endl;
    //throw invalid_argument("test");

    //ifstream test("default/nginx.conf");
    //if (!getline(test, ttt, '{'))
    //    cout << "errror\n";
    cout << ttt.size();
}